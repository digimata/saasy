---
id: SSY-0011
title: Scaffold packages/email (React Email + Resend) with welcome + receipt templates
status: backlog
priority: 2
project: saasy
path: projects/saasy/.tasks/T-0011.md
created_at: 2026-04-21T23:58:12.399Z
updated_at: 2026-04-22T00:36:46.871Z
generated_by: kdb
---

# SSY-0011 — Scaffold packages/email (React Email + Resend) with welcome + receipt templates

_Generated by `kdb`. Do not edit by hand._

Scaffold a workspace-level `packages/email` for app-owned transactional emails in the b2c-clerk-vite template. Clerk already handles auth-flow emails (verify, reset, magic link) — this package is for emails the app itself sends.

## Context (as of 2026-04-22, post-SSY-0008)

Repo layout: `saasy/` is the git root; `b2c-clerk-vite/` is the Vite + React + Clerk + Hono + Drizzle template. `saasy/b2b-betterauth-next/` sits next to it as a separate template (has its own `packages/email/` for reference, but it's tailored to better-auth flows — OTP, invitation — which are NOT needed here).

Prior work landed in commit `87bbf30`:
- `packages/billing` (Stripe, user-scoped customers/subscriptions)
- `/billing/{checkout,portal,state,invoices}` endpoints
- `POST /webhooks/stripe` with TODO branch for `invoice.paid` (good hook point for receipt email)
- `POST /webhooks/clerk` handles `user.created` / `user.updated` / `user.deleted` (good hook point for welcome email)

## Deliverables

1. `packages/email/` workspace package
   - Deps: `react-email`, `@react-email/components`, `resend`
   - Structure mirrors existing packages (`package.json`, `tsconfig.json`, `src/`)
   - Exports: `sendEmail(to, template, props)` plus the two template React components

2. Two templates
   - `WelcomeEmail` — sent on Clerk `user.created`, after the DB row is upserted
   - `ReceiptEmail` — sent on Stripe `invoice.paid`, includes amount + hosted invoice link

3. Env (all optional, template works without them)
   - `RESEND_API_KEY` — if absent, `sendEmail` logs the rendered template via pino at info level instead of sending; keeps the scaffold runnable locally without a Resend account
   - `EMAIL_FROM` — e.g. \`noreply@example.com\`
   - Add to `apps/api/src/lib/env.ts` (zod schema) and `apps/api/.env.example` (commented)

4. Wire triggers
   - `apps/api/src/api/webhooks/clerk.ts`: on `user.created`, call `sendEmail(primaryEmail, WelcomeEmail, { firstName })` after the db insert
   - `apps/api/src/api/webhooks/stripe.ts`: in the currently-empty `invoice.paid` branch, look up the customer → user → email, then `sendEmail(user.email, ReceiptEmail, { ... })`

5. Preview script (optional but nice)
   - `packages/email` script \`email:preview\` runs \`react-email dev\` — lets you iterate on templates in a local server

## Out of scope
- Any queueing / retry infrastructure (direct Resend calls are fine for a template)
- Marketing emails, lists, unsubscribe flows
- Custom SMTP / non-Resend providers

## Done when
- \`pnpm check-types\` passes for the new package and the two webhook changes
- Locally: creating a user in Clerk → webhook fires → welcome email is logged (since no RESEND_API_KEY by default)
- Locally: Stripe CLI forward \`invoice.paid\` event → receipt email is logged
- \`docs/testing.md\` gets a short section on how to test emails with/without Resend
