// ---------------------------
// projects/saasy/apps/web/components/auth/captcha/captcha.tsx
//
// export const Captcha    L12
// ---------------------------

"use client";

import { forwardRef } from "react";

// Stub — captcha not enabled. Renders nothing.
export const Captcha = forwardRef<unknown, Record<string, unknown>>(
  function Captcha(_props, _ref) {
    return null;
  }
);
